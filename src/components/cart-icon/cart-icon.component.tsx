import {Container, Bag, Count} from "./cart-icon.styles";

const CartIcon = () => {
  return (
    <Container>
      <Bag />
      <Count>50</Count>
    </Container>
  );
};

export default CartIcon;
